The paper figures come only from the accepted corrected synthetic review
snapshot:
\begin{quote}
\texttt{experiments/tier1-synthetic/corrected\_synthetic\_artifacts/}\\
\texttt{review\_snapshot/p1\_corrected\_synthetic\_paper\_ac56a7c/}
\end{quote}

The provenance policy is:
\begin{itemize}
\item figure sources are explicit review snapshots from accepted PR~\#4;
\item the source of truth is the corresponding code, manifests, and CI
regeneration checks;
\item legacy \texttt{ALL\_RUNS.json} files are deprecated and are not used by
the corrected aggregate pipeline;
\item all stochastic summaries in the review snapshot are tied to explicit run
IDs and manifest paths.
\end{itemize}

Figure-level provenance is summarized in \texttt{paper/figures/README.md}. The
accepted exact-Fira fixture summary and LDAdam status note are included only as
appendix boundary material; they are not broadened into optimizer claims.
