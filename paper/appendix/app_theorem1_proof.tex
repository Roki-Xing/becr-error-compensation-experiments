\textbf{Status.} TODO: integrate the full accepted proof text from GPT Pro V3
without renumbering.

\medskip
\noindent
The main paper uses the corrected positive-$\epsilon$ theorem-regime dynamics
implemented in \texttt{corrected\_synthetic/core.py}. The accepted diagnostic
verifies the theorem conditions
\[
\eta a < 2\epsilon_a,\qquad
\eta b M_0 \le \frac{1}{2},\qquad
M_0 = \max_{0\le r\le a|x_0|}\frac{r}{(r+\epsilon_a)(r+\epsilon_s)}.
\]
TODO: insert the full decay argument for $x_t$, the summability argument for
$\phi_t$, and the nonzero limit argument for $y_t$.
